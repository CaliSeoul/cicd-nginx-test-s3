name: AWS CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ap-southeast-2

      # 1. S3 배포
      - name: S3 Deploy
        run: aws s3 sync ./frontend s3://${{ secrets.S3_BUCKET_NAME }} --delete

      # 2. ECR 빌드 & 푸시
      - name: ECR Login & Push
        run: |
          aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin ${{ secrets.ECR_URI }}
          docker build -t ${{ secrets.ECR_URI }}:latest ./backend
          docker push ${{ secrets.ECR_URI }}:latest

      # 3. SSM 명령 전송 (EC2 배포)
      - name: SSM Deploy
        run: |
          aws ssm send-command \
            --instance-ids ${{ secrets.EC2_INSTANCE_ID }} \
            --document-name "AWS-RunShellScript" \
            --parameters "commands=[
              'aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin ${{ secrets.ECR_URI }}',
              'docker pull ${{ secrets.ECR_URI }}:latest',
              'docker stop app || true && docker rm app || true',
              'docker run -d --name app -p 80:80 ${{ secrets.ECR_URI }}:latest'
            ]"